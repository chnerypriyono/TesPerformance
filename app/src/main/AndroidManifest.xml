<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tesperformance">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".MainApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".NestedPagerRecyclerViewActivity">

        </activity>

        <activity android:name=".ImageCarouselRecyclerViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ImageNetworkRecyclerViewActivity">

        </activity>

        <activity android:name=".ImageRecyclerViewActivity">

        </activity>

        <activity android:name=".TextRecyclerViewActivity">

        </activity>

        <activity android:name=".EmptyRecyclerViewActivity">

        </activity>


        <activity android:name=".SingleTextViewActivity">

        </activity>
    </application>

</manifest>